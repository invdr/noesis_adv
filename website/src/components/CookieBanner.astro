<aside class="cookie" data-cookie-banner aria-label="Уведомление о cookie" hidden>
  <p>
    Сайт использует cookie для работы интерфейса и обезличенной аналитики.
    Подробнее — в <a href="/privacy">политике конфиденциальности</a>.
  </p>
  <button type="button" data-cookie-accept>Понятно</button>
</aside>

<style>
  .cookie {
    position: fixed;
    z-index: 300;
    right: 18px;
    bottom: 18px;
    display: grid;
    width: min(520px, calc(100% - 36px));
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 18px;
    padding: 18px;
    color: #fbfaf5;
    background: var(--dark);
    border: 1px solid var(--dark-line);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow);
  }
  .cookie p { margin: 0; font-size: 13px; line-height: 1.5; }
  .cookie a { color: var(--accent-light); text-decoration: underline; text-underline-offset: 3px; }
  .cookie button {
    min-width: 86px;
    min-height: 44px;
    padding: 0 16px;
    color: #fbfaf5;
    background: var(--accent);
    border-radius: var(--radius-sm);
    font-size: 13px;
    font-weight: 600;
  }
  .cookie button:hover { background: var(--accent-hover); }

  @media (max-width: 560px) {
    .cookie {
      right: 10px;
      bottom: 10px;
      width: calc(100% - 20px);
      grid-template-columns: 1fr;
    }
    .cookie button { width: 100%; }
  }
</style>
