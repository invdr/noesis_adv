<a class="selection-bar" href="/selection" data-mobile-selection hidden>
  <span>Открыть подборку</span>
  <span class="selection-bar__meta"><b data-shortlist-count>0</b> <span data-shortlist-word>позиций</span></span>
</a>

<style>
  /* На десктопе панели нет вовсе. */
  .selection-bar { display: none; }

  @media (max-width: 760px) {
    /* Медиа-блок обязан вернуть display: одной специфичности с базовым
       правилом, поэтому раньше выигрывало `none` и панель не появлялась
       никогда — при этом скрипт снимал hidden и резервировал под неё 76px
       внизу каждой страницы. */
    .selection-bar {
      display: flex;
      position: fixed;
      z-index: 150;
      right: 10px;
      bottom: 10px;
      left: 10px;
      min-height: 58px;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 0 18px;
      color: #fbfaf5;
      background: var(--accent);
      border: 1px solid rgba(251, 250, 245, 0.3);
      border-radius: var(--radius-md);
      box-shadow: 0 16px 36px rgba(85, 0, 0, 0.28);
      font-size: 14px;
      font-weight: 600;
    }

    /* Скрипт управляет видимостью через hidden: пустая подборка — нет панели.
       Специфичность атрибута выше, чем у класса, поэтому правило работает. */
    .selection-bar[hidden] {
      display: none;
    }

    .selection-bar__meta {
      display: inline-flex;
      align-items: baseline;
      gap: 5px;
      color: rgba(251, 250, 245, 0.74);
      font-size: 11px;
      font-weight: 500;
    }
    .selection-bar__meta b { color: #fbfaf5; font-family: var(--font-mono); font-size: 14px; }
  }
</style>
