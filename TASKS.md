# План работ — Noesis

Статус и декомпозиция. Продуктовые рамки — в [PRD.md](./PRD.md).

Легенда: ✅ готово · 🔄 в работе · ⬜ предстоит.

---

## Этап 0. Перенос и подготовка базы

Взять tower-site за основу, снять неприменимое, отбрендировать.

- ✅ Перенос монорепо tower-site → noesis_adv (website/webapp/backend/contracts/
  infra), без `node_modules` и дизайн-референсов.
- ✅ Удаление **калькулятора рассрочки** целиком (модели `CalcComplex`/
  `CalculatorSettings`/`CalcSnapshot`, backend/contracts/webapp, секция и JS на
  лендинге, веб-источник заявок «calculator», поля настроек сайта).
- ✅ Удаление **планировок квартир** (`ProjectLayout`, backend/layouts,
  contracts/layout, webapp/ProjectLayouts, секция на странице объекта).
- ✅ `site-stats`: убрана метрика «цена за м²» (зависела от калькулятора).
- ✅ Схлопывание миграций Prisma в одну начальную (продовой БД ещё нет).
- ✅ **Перекраска**: тёмно-красный акцент `#A4161A`, светлый фон, синий убран
  (website `--accent*`, webapp `--primary`, палитры этапов/бейджей, графики).
- ✅ **Ребрендинг**: пакеты `@noesis/*`, глобали `window.NOESIS_*`, бренд
  «Noesis» в чроме; инфра (сервис-сборщик, БД, пути, домен-плейсхолдер).
- ✅ Переработка документации под новый домен (README, CLAUDE, PRD, TASKS, docs).
- ✅ Гейт зелёный: typecheck всего монорепо; тесты contracts + webapp.

---

## Этап 1. Каталог рекламных конструкций

Превратить унаследованный `Project` в **рекламную конструкцию**.

- ✅ Контракты: схема конструкции (`construction.ts`) — код, адрес, район,
  гео-координаты, формат/габариты, сторона, подсветка, охват (GRP/трафик),
  цена за месяц, фото, статус. Slug/бейджи вынесены в `common.ts`.
- ✅ Backend: модель `Project`→`Construction` (+ `ConstructionImage`), init-
  миграция пересобрана, сервис/DTO/роуты (`/api/constructions`,
  `/api/public/constructions`), сид демо-каталога.
- ✅ Webapp: редактор конструкции в CRM (формат, сторона, подсветка, гео,
  охват, цена/мес, фото, бейджи).
- ✅ Терминология в CRM/бэке: `Lead.constructionId`, аналитика, источники,
  «владелец сети» в редакторе.
- ✅ Публичный сайт: каталог и карточки переведены на конструкции
  (`/api/public/constructions`, `/constructions/<slug>`), заявки со страниц
  отправляют `constructionId`; legacy `/api/public/projects` удалён.
- ✅ Быстрая карта на главной: Яндекс.Карты с центром в Грозном, пины берутся
  из координат конструкций; ключ задаётся через `PUBLIC_YANDEX_MAPS_API_KEY`.

## Этап 2. Инвентарь и бронирование по периодам

Ядро продаж наружной рекламы — новая сущность, которой в базе нет.

- ✅ Контракты: `Booking` (конструкция × сторона × рекламодатель/служебная причина
  × период × цена-снимок × статус), справочники брендов/служебных причин, helper
  плавающих месяцев.
- ✅ Backend MVP: модели Prisma, API `/api/bookings`, сервисная проверка
  пересечений периодов в `Serializable`-транзакции, CRUD справочников, сид причин
  служебных броней.
- ✅ Webapp MVP: раздел **«Брони»** с таймлайном/Гантом, фильтрами и формой
  создания/редактирования; справочники «Бренды» и «Причины броней».
- ✅ Расширить модель инвентаря до продаваемых сторон конструкции: 1/2/3 стороны
  (`A/B/C`), отдельные параметры стороны (направление, цена, трафик/GRP, фото),
  цена конструкции как дефолт для стороны.
- ✅ Аналитика: загрузка/выручка по конструкциям и периодам.
- ✅ Публичная занятость: агрегат свободно/занято на выбранный период без клиента/
  бренда/цены (полный каталог-карта — Этап 3/5).
- ✅ Напоминание о сроке брони в «Мой день» + Telegram (Развилки 3/6): в «Моём
  дне» — блок «Сроки броней» (просрочено/сегодня/ближайшие 7 дней, lazy-on-read);
  Telegram-дайджест ответственному (fallback — создатель) через идемпотентную
  внутреннюю ручку по суточному cron (`REMINDER_CRON_TOKEN`, дедуп
  `reminderNotifiedAt`). Отменённые/завершённые и брони без напоминания
  исключены. Настройка cron — см. docs/DEPLOYMENT_VPS.md.

## Этап 3. Карта города

- ✅ Публичный каталог с картой Грозного и пинами конструкций: страница
  `/catalog` — фильтры формат/цена/район + занятость на период, пины по статусу
  сторон (свободна/частично/занята), список синхронизирован с картой. Каталог
  запечён в статику (SEO), занятость дотягивается из `/api/public/
  construction-availability` без изменений бэкенда.
- ✅ Карта в карточке конструкции CRM: пикер координат на Яндекс-карте (клик/
  перетаскивание маркера + поиск по адресу), синхронизирован с полями широты/
  долготы; ключ карт переиспользуется с публичного (`VITE_YANDEX_MAPS_API_KEY`
  ← `PUBLIC_YANDEX_MAPS_API_KEY`). Без ключа — прежний ручной ввод.

## Этап 4. Сделки и документы

- ✅ Сделка из выигранной заявки: вкладка «Брони и документы» на карточке заявки
  — привязанные брони (read-only) + прикрепление закрывающих документов
  (договор/счёт/акт/иное) с явной отметкой «без документов». «Сделка» = заявка,
  отдельной сущности нет. Генерация документов из шаблонов — отдельный заход
  (по решению — пока только прикрепление).
- ✅ Фотоотчёты о размещении помесячно: отдельный внутренний альбом на бронь и
  календарный месяц, дата/комментарий/несколько фото, просмотр также в сделке.
  Файлы закрыты от публичной раздачи и открываются только через сессию CRM.
- ✅ Контрагенты и реквизиты.
  - Контрагент — человек или компания с независимыми ролями «клиент» и/или
    «партнёр»; представитель может быть связан с компанией.
  - Добавлены паспортные, юридические и банковские реквизиты, раздел CRM
    «Контрагенты и реквизиты» и аналитика партнёров/компаний.
  - Миграция `20260711051739_counterparties_and_requisites` безопасно переносит
    прежние записи и связи: client → клиент, realtor → партнёр, agency → компания.
  - Вместо жёстких типов «риелтор»/«агентство»: контрагент — человек или
    компания, с ролями «клиент» и/или «партнёр». Партнёр-человек при
    необходимости связан с компанией.
  - Перенести существующие данные без потери истории: риелтор → партнёр,
    агентство → компания; в интерфейсе и аналитике использовать новые названия.
  - Реквизиты компании: полное наименование, ИНН, КПП, ОГРН, адрес, руководитель,
    контакты, банк, БИК, расчётный и корреспондентский счета; для физлица —
    ФИО, контакты и необходимые платёжные/паспортные данные.
  - Для будущих договора, счёта и акта сохранять снимок реквизитов на момент
    формирования документа.

## Этап 4.5. Финансы

- ⬜ Фактические поступления: отдельные записи денег с датой, суммой, бронью и
  конструкцией. Цена брони остаётся прогнозом и не участвует в распределении.
- ⬜ Расходы: редактируемые статьи (монтаж, демонтаж, очистка, электропитание,
  подсветка, ремонт, обслуживание и др.) и записи расходов с датой, суммой,
  комментарием и привязкой к конструкции; при необходимости — к стороне/брони.
- ⬜ Участники: «я», инвестор и дополнительные участники с долей от чистого
  дохода и периодом действия доли. Доступ к финансовым данным — только admin.
- ⬜ Распределения: помесячный расчёт «фактические поступления − подтверждённые
  расходы», фиксация долей при закрытии периода, учёт фактических выплат и
  остатка к выплате.
- ⬜ Новый пункт левого меню «Финансы»: сводка, поступления, расходы, участники
  и распределения; отчёты по конструкции и по периоду.

## Этап 5. Редизайн публичного сайта

- ⬜ Дизайн под наружную рекламу (hero, каталог-карта, форматы/охваты, кейсы) на
  той же data-driven архитектуре и конвейере публикации.
- ✅ Базовый копирайт, дефолты настроек и демо-сид переведены под наружную
  рекламу; полноценный визуальный редизайн остаётся отдельной задачей.
- ⬜ Заменить логотип-плейсхолдер, обновить OG/иконки.

## Этап 6. Прод

- ⬜ Провижининг VPS: сервер, домен, TLS, секреты (`infra/.env`), сервис-сборщик
  `noesis-site-builder`, бэкапы по cron.
- ✅ Первый деплой (`bun run deploy:vps` для чистого Docker VPS или
  `bun run deploy:vps:no-docker` для подселения рядом с существующим nginx/PG),
  проверка публикации сайта из CRM.

---

## Вне текущих рамок

- Мобильное приложение (`mobile/` — заготовка Expo).
- Интеграции с внешними системами учёта/бухгалтерии.
