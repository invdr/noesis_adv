# Передача контекста между сессиями

> **Живой файл.** Источник правды по текущему фокусу работы. Обновляй его по ходу
> (решения, открытые вопросы, состояние), чтобы новая сессия поднимала контекст
> без потерь. Историческую канву держим в TASKS.md/PRD.md, а здесь — «что прямо
> сейчас в руках».

**Последнее обновление:** 2026-07-10
**Текущий фокус:** Этап 4 — сделки и документы (начали с прикрепления документов).
**Стадия:** Этапы 2, 3 и первый проход Этапа 4 реализованы в ветке и не выкачены
на прод (нужен деплой: миграции + сборка сайта/CRM). Этап 2: стороны/брони/
аналитика/публичная занятость + эдж-кейсы + напоминания. Этап 3: CRM-пикер
координат + публичная фильтр-карта `/catalog`. Этап 4 (проход 1): вкладка «Брони
и документы» на заявке.
**Ветка:** `claude/edge-cases-review-2wynjt`.

## Последующие замечания независимого ревью (2026-07-10)

- Закрыты: `no-docker` ограничивает анонимный `POST /api/leads` до 1 МБ; Telegram-дайджест помечает напоминания только после успешного ответа Telegram; ручная дата напоминания в новой брони не перетирается при изменении периода. Для Telegram добавлен регрессионный тест на не-OK ответ.
- Открытая продуктовая развилка: документы сделки пока получают публичные URL из `/files/`; нужно выбрать auth-gated скачивание или короткоживущие подписанные URL перед изменением модели выдачи и DTO.
- Открытая продуктовая развилка: подтвердить, должны ли черновики конструкций попадать во внутреннюю аналитику инвентаря. Без явного решения текущий фильтр не менять.

## Этап 4: сделки и документы (2026-07-10)

Развилки: A1 — «сделка» = выигранная заявка (без новой сущности); B2 —
**прикрепление** документов (генерация отложена по решению) + явная отметка
«без документов»; C (реквизиты) и D (фотоотчёты) — следующими заходами.

### ✅ Проход 1 — вкладка «Брони и документы» на заявке

- Prisma: enum `DealDocumentType` (contract/invoice/act/other), модель
  `DealDocument` (leadId, type, name, assetId, createdBy), поле
  `Lead.dealNoDocuments`. Миграция `20260710160000_deal_documents`.
- Контракт `packages/contracts/src/deal.ts`: типы документа, `dealBookingSummary`;
  `leadDetailSchema` расширен `bookings` (read-only) + `dealDocuments` +
  `dealNoDocuments`.
- Backend (`backend/src/leads/deal-*.ts`): `POST /api/leads/:id/documents`
  (multipart, файл или скан-изображение), `DELETE .../documents/:docId`,
  `PATCH /api/leads/:id/deal` (`{noDocuments}`); все возвращают обновлённую
  карточку. Guard — `assertCanEdit` (экспортирован из lead-service). Прикрепление
  документа снимает `dealNoDocuments`. `getLeadDetail` тянет брони + документы.
- CRM (`webapp/src/leads/DealSection.tsx`): в карточке заявки блок «Брони и
  документы» — список привязанных броней, список документов (тип, скачать,
  удалить), форма прикрепления (тип/название/файл) и чекбокс «без документов».
- Тесты: контракт `deal.test.ts`, DTO `deal-dto.test.ts` (инклюзивный endDate),
  роут-тест `deal-routes.test.ts` (401/403/422/404 через реальный `createApp`).
- Гейт зелёный: typecheck + test (contracts 109 / backend 241 / webapp 35),
  прод-сборка `build:webapp` ок. БД-e2e не гоняли (в песочнице нет Postgres).

### ⬜ Следующее по Этапу 4
- C: реквизиты сторон — поля юрлица на `Contact` (клиент физлицо/юрлицо, зеркально
  `Developer`) + реквизиты агентства в настройках сайта.
- D: помесячные фотоотчёты размещения на бронь (переиспользовать `ProgressAlbum`).
- (позже) B1: генерация договора/счёта/акта из шаблонов.

⚠️ **Деплой-заметка:** на проде применить новые миграции
(`20260710120000_booking_reminder_notified`, `20260710160000_deal_documents`)
через `deploy-no-docker.sh` (`prisma migrate deploy` на старте).

## Этап 3: карта города (2026-07-10)

Этап 2 закрыт полностью (стороны, брони, аналитика, публичная занятость,
эдж-кейсы, напоминания). Перешли к Этапу 3.

### ✅ CRM-пикер координат (item «карта в карточке CRM»)

- `webapp/src/projects/CoordinatePicker.tsx` + лоадер
  `webapp/src/shared/yandexMaps.ts`: Яндекс-карта в редакторе конструкции —
  клик по карте / перетаскивание маркера ставит точку, поиск по адресу
  (геокодер) центрирует и ставит маркер. Двусторонняя синхронизация с ручными
  полями широты/долготы; кнопка «Очистить координаты» сбрасывает пару.
- Ключ карт — тот же, что у сайта: сборка CRM получает
  `VITE_YANDEX_MAPS_API_KEY`, в деплое он берётся из `PUBLIC_YANDEX_MAPS_API_KEY`
  (`deploy.sh`, `deploy-no-docker.sh`). Отдельно на VPS ничего задавать не надо —
  если ключ карты уже настроен для сайта, пикер заработает. Без ключа компонент
  не рендерится, остаётся прежний ручной ввод lat/lng.
- Гейт: typecheck + test зелёные; проверена и прод-сборка `build:webapp`
  (ключ инлайнится, лоадер попадает в чанк ProjectsView).

### ✅ Публичная фильтр-карта — страница `/catalog` (Этап 3 закрыт)

Реализовано по рекомендациям, но развилку №2 пересмотрел: вместо расширения
бэкенд-эндпоинта — запекаем богатый каталог в страницу (SEO + мгновенный рендер),
а live-занятость тянем существующей ручкой Этапа 2. **Бэкенд не менялся.**

- Новая страница `website/src/pages/catalog.astro` + скрипт
  `website/public/js/catalog.js` (ванильный, в стиле `main.js`).
- Каталожные поля (формат/цена/район/координаты/стороны/обложка) запекаются на
  сборке через `toCatalogItem` (`website/src/lib/api.ts`); карточки — в статике.
- Фильтры: формат, район, цена от/до, период (с/по, инклюзивно), «только
  свободные на период». Занятость дотягивается из
  `/api/public/construction-availability?from&to` в браузере, мёрж по id.
- Пины по агрегату сторон: свободна (есть полностью свободная сторона) → зелёный,
  частично → оранжевый, занята (все стороны заняты) → серый; без периода — красный.
- Список синхронизирован с картой: клик по пину подсвечивает/скроллит карточку,
  кнопка «Показать на карте» центрирует и открывает балун.
- Навигация «Карта» в шапке ведёт на `/catalog`; тизер `#map` на главной — тоже;
  `/catalog` добавлен в sitemap.
- Ключ карт — публичный `PUBLIC_YANDEX_MAPS_API_KEY` (как и раньше у сайта).
- Проверка: typecheck зелёный; смоук-`astro build` против мок-API собрал
  `/catalog/index.html` (3 карточки, фильтры, конфиг с сторонами, sitemap с
  `/catalog`). Полный визуальный прогон карты/занятости — на прод-деплое.

Замечание по vanilla JS: сайт-скрипты не покрыты юнит-тестами (как и `main.js`);
логика агрегата занятости и инклюзив↔эксклюзив периода — в `catalog.js`.

## Эдж-кейс ревью Этапа 2 (2026-07-10)

Прошёлся по броням/сторонам/аналитике/публичной занятости. Что нашёл и сделал:

- ✅ **B1.** Форма брони (`webapp/.../BookingsView.tsx`): дефолт `reminderAt` не
  следовал за периодом (инициализатор `useState` от «+1 мес от сегодня»). Добавил
  эффект: для новой брони напоминание пересчитывается от `endDate` при смене
  `startDate`/`durationMonths`; существующую бронь не трогаем (ручная дата).
- ✅ **B3.** `inventoryAnalyticsQuerySchema` (общая с публичной занятостью) имела
  только `from < to`. Добавил потолок окна `MAX_ANALYTICS_WINDOW_MONTHS = 24`,
  чтобы неавторизованный `/api/public/construction-availability` нельзя было
  дёрнуть с окном в сотни лет.
- ✅ **B4.** Поиск в списке броней (`booking-service.ts`) не покрывал причину
  служебной брони — добавил `serviceReason.name` в `where.OR`.
- Тесты: `packages/contracts/test/inventory-analytics.test.ts` (потолок окна),
  доп. кейс в `backend/test/booking-service.test.ts` (поиск по причине).

Проверено и **НЕ баг** (оставил как есть): архивация по полуоткрытому `endDate`;
гонки пересечений под Serializable+SSI; запрет уменьшения `sideCount` при бронях
(ловится свежей строкой и FK-гонкой P2003); кламп конца месяца; стык-встык;
отменённая бронь не занимает; дефолт напоминания «за 7 дней до `endDate`»
совпадает со спекой Развилки 6.

### ✅ Крупная развилка закрыта — напоминание о сроке брони работает (2026-07-10)

Достроил консюмер `Booking.reminderAt` — веб + Telegram, по архитектуре проекта
(lazy-on-read + «выстрелил-и-забыл», без фоновых демонов):

- **Веб «Мой день»**: блок «Сроки броней» (`AgendaView.tsx`) — активные брони с
  напоминанием, разложенные по дню МСК на просрочено/сегодня/ближайшие 7 дней.
  Бэкенд: `GET /api/bookings/reminders` → `getBookingReminders` в
  `booking-reminder-service.ts`. Видимость: менеджер — свои (ответственный или,
  если не задан, создатель); admin — вся команда / свои по `scope`.
- **Telegram-дайджест**: `sendDueBookingReminders` + внутренняя ручка
  `POST /api/internal/bookings/reminders/notify` (токен `REMINDER_CRON_TOKEN`,
  заголовок `X-Reminder-Token`, паттерн как у site-build). Дёргается суточным
  cron на VPS (см. docs/DEPLOYMENT_VPS.md). Адресат — личный чат ответственного
  (fallback — создатель); без `telegramChatId` бронь пропускается (уведомит
  позже). Дедуп — новое поле `Booking.reminderNotifiedAt`; перенос срока
  сбрасывает флаг (перевзвешивание — в `saveBooking`).
- **B2 закрыт**: отменённые/завершённые и брони без `reminderAt` в напоминания не
  попадают (статус-фильтр `booked`/`onAir`).
- **Миграция** `20260710120000_booking_reminder_notified`: добавляет
  `reminderNotifiedAt` + бэкфилл (помечает уже наступившие/неактивные напоминания
  «уведомлёнными»), чтобы первый cron-запуск не разослал историю задним числом.
- Тесты: `booking-reminder-service.test.ts` (группировка/видимость/дедуп/адресат),
  re-arm дедупа в `booking-service.test.ts`.

⚠️ **Прод после деплоя:** применить миграцию (`prisma migrate deploy` внутри
`deploy-no-docker.sh`); для Telegram — задать `REMINDER_CRON_TOKEN` (+
`TELEGRAM_BOT_TOKEN`) в env и добавить cron-строку (docs/DEPLOYMENT_VPS.md,
раздел «Напоминания о сроке брони»). Без токена рассылка выключена (404),
веб-«Мой день» работает всегда.

## Как работаем над Этапом 2

Продукт (владелец) отвечает с точки зрения UX/практики продаж; ассистент даёт
техническое решение в формате «эдж-кейсы + варианты», идём **последовательно** по
развилкам. Модель проектируем «контракты-первыми»: схема в `packages/contracts`
→ backend service → DTO → webapp. Новая сущность — `Booking`.

## Решённые развилки

### ✅ 1. Единица брони — плавающий месяц
- Минимум 1 месяц; старт — **любой день** (напр. `05.05–05.06`, `21.07–21.08`).
- Срок **кратен месяцу** (целые месяцы, без дробей). Цена = цена/мес × число месяцев.
- Хранение — интервал дат `[начало, конец)` (полуоткрытый, стык встык не пересекается).
- Стык «31-х чисел»: `31.01 + 1 мес → 28.02` (клампим к концу месяца).
- ⚠️ Следствие: сетка занятости = **таймлайн/Гант** (полосы), а не таблица месяц-в-месяц.

### ✅ 2. Стороны конструкции — отдельные продаваемые поверхности
- Конструкция может быть **одно-, двух- или трёхсторонней**: `A`, `A/B`,
  `A/B/C` (куб).
- Стороны продаются раздельно и могут иметь свои параметры: направление/описание,
  цена, трафик/GRP, фото. Отдельное публичное название стороны пока не нужно:
  достаточно кода `A/B/C` + описания.
- Цена конструкции остаётся дефолтом для стороны. Если у стороны есть своя цена —
  используем её; если нет — цену конструкции; если нет обеих — «Цена по запросу».
- Целевая модель: отдельная сущность стороны конструкции, а не только
  `sideCount + side`. Бронь должна занимать конкретную сторону.
- У односторонней конструкции в UX выбор стороны не показываем, но технически это
  сторона `A`.
- Нельзя уменьшить число сторон, если на удаляемой стороне есть любые брони:
  `3 → 2` запрещён при бронях на `C`, `2 → 1` — при бронях на `B`.

### ✅ 3. Жизненный цикл брони
- Статусы: **Забронировано → В эфире → Завершено** + **Отменена**. Всё **вручную**.
  «В эфире» = плакат физически смонтирован; «Завершено» = по факту демонтажа.
- Отдельного «Подтверждено» **нет**. Мягкой брони/опции с TTL **нет**.
- **Занятость считается по датам** `[начало, конец)`: любая не-отменённая бронь
  занимает сторону. «Завершено» — операционная отметка, занятость не меняет;
  «Отменена» — убирает из занятости.
- ➕ **Новое требование:** напоминание менеджеру, что у брони **подходит срок**
  (веб-интерфейс + Telegram). Переиспользуем «следующий контакт» + «Мой день» +
  TG-уведомления. Параметры (за сколько дней, кому) — в развилке 6.

### ✅ 4. Кого/что бронируем
- **Тип брони** `kind: commercial | service`:
  - `commercial` — обязателен клиент-`Contact` (плательщик);
  - `service` — без клиента, обязательна **причина** из справочника (ремонт / своя
    реклама / резерв / демонтаж). Занимает (сторона+период) как обычная бронь; в
    аналитику **выручки** не входит, в **загрузку** — входит.
- **«Что размещаем»** — справочник **брендов** (`brandId`) + опц. свободный текст
  кампании (`campaignNote`), отдельно от плательщика. Бренд с **инлайн-быстрым
  добавлением** (как контакты), чтобы не блокировать менеджера; на публичной сетке
  бренд/клиент **не** показываем.
- **Новые справочники** (раздел «Справочники» CRM, правит admin): причины
  служебных броней, бренды.
- Бронь — **самостоятельный объект**: создаётся напрямую из сетки; `leadId`/сделка —
  **необязательная** привязка. Обязательны `Construction + side + период + статус`
  (+ клиент для `commercial`). Переход в сделку — Этап 4.

### ✅ 5. Цена брони
- `Construction.pricePerMonth` остаётся **каталожной ценой по умолчанию** для
  быстрого расчёта формы. Сторона может переопределить цену; при создании брони
  стартовая цена берётся с выбранной стороны, иначе с конструкции.
- Бронь хранит **снимок цены**, независимый от будущих изменений конструкции:
  `basePricePerMonth` (скопированная цена/мес, nullable), `durationMonths`
  (целое число месяцев), `totalPrice` (финальная сумма, nullable) и опциональный
  `priceNote` (пояснение скидки/ручного расчёта).
- Дефолт при создании: `totalPrice = basePricePerMonth × durationMonths`, если у
  выбранной стороны/конструкции задана цена. Менеджер может вручную изменить
  итоговую сумму; отдельную модель скидок/коэффициентов пока не вводим — аудит
  виден как разница между базовым снимком и финальной суммой + комментарий.
- Если `Construction.pricePerMonth = null` («по запросу»), коммерческую бронь можно
  создать без цены (`basePricePerMonth = null`, `totalPrice = null`) и заполнить
  сумму позже. В аналитику выручки такие брони не входят до появления `totalPrice`;
  отдельно показываем счётчик броней без цены.
- Служебные брони (`service`) выручку не формируют: `totalPrice` для них не нужен,
  но в загрузку/занятость они входят как обычные.
- Изменение цены на конструкции после создания **не меняет** существующие брони.
  При переносе/правке периода пересчёт цены должен быть явным действием менеджера,
  а не неявным сайд-эффектом сохранения.

### ✅ 6. Время
- Бэкофилл прошлых размещений **разрешён**: брони можно создавать с датами в прошлом
  и сразу нужным ручным статусом (`booked`, `onAir`, `completed`, `cancelled`).
- Период хранится как даты без времени: `startDate`, `endDate` (`[start, end)`).
  UI вводит локальные календарные даты; backend валидирует длительность целыми
  месяцами через общий helper и не делает автопереходов по текущей дате.
- У брони есть ответственный `managerId` (по умолчанию — текущий пользователь) и
  напоминание `reminderAt`. Дефолт — за **7 дней** до `endDate`; менеджер может
  отключить напоминание (`null`) или поставить свою дату. В «Мой день» и Telegram
  попадает ответственный; если он не задан, fallback — создатель брони.
- Правка/перенос текущей брони разрешены для всех не удалённых записей, если новый
  период не пересекается с занятостью той же стороны. Статусы меняются вручную.
  Отмена (`cancelled`) снимает занятость; физического удаления брони в обычном UI нет.

### ✅ 7. Целостность
- Пересечения ловим **в сервисе**: транзакция `Serializable`, поиск конфликтов по
  конкретной стороне конструкции и периоду `[startDate, endDate)` среди всех
  не-отменённых броней.
- Postgres `EXCLUDE` пока **не используем**: Prisma не поддерживает его декларативно,
  а в проекте принято не писать SQL миграций руками без необходимости. При единственном
  инстансе VPS сервисная проверка + `Serializable` достаточны для MVP; если появятся
  конкурентные продажи/несколько инстансов, вернёмся к `EXCLUDE`.
- Архивация конструкции блокируется, если есть будущие/текущие не-отменённые брони
  (`endDate` позже сегодняшнего дня). Исторические завершённые брони не мешают
  архиву. Жёсткое удаление конструкции блокируется при любых бронях.

### ✅ 8. Сетка занятости
- MVP CRM — отдельный раздел **«Брони»**: таймлайн/Гант с окном по датам, фильтрами
  по статусу/типу/конструкции/поиску и формой создания/редактирования брони.
  Единица строки — продаваемая сторона конструкции (`A/B/C`). У односторонней
  конструкции в интерфейсе строка может называться просто конструкцией.
- Форма брони открывается из раздела «Брони» и создаёт самостоятельную бронь без
  обязательной заявки. `leadId` остаётся nullable для будущего перехода в сделку.
- Публичная видимость занятости на Этапе 2 — только технический агрегат
  «свободно/занято на выбранный период», без клиента/бренда/цены. Полноценный
  публичный каталог с картой — Этап 3/5.

### ✅ 9. Аналитика загрузки/выручки
- Выручка MVP — **плановая**: все не-отменённые коммерческие брони с `totalPrice`
  (`booked`, `onAir`, `completed`), служебные и отменённые не входят.
- Если бронь длиннее выбранного периода, выручку распределяем пропорционально
  дням пересечения с периодом. Брони без цены участвуют в загрузке, но не в
  выручке; считаем их отдельно.
- Загрузка считается точно по сторонам/дням; UX-статусы: свободна, частично
  занята, занята + процент загрузки.
- В интерфейсе даты занятости показываем включительно: внутренний период
  `01.08–01.09` отображается как `01.08–31.08`.
- Первый экран CRM-аналитики: период от/до, быстрые кнопки (текущий месяц,
  следующий месяц, 3 месяца, 6 месяцев), карточки загрузки/выручки/свободных
  сторон/броней без цены и таблица по сторонам, сгруппированная по конструкциям.
- Архивные конструкции/стороны скрыты по умолчанию, но попадают в аналитику,
  если в выбранном периоде по ним были брони.

### ✅ 10. Публичная занятость и заявки
- Публичная карта: один пин = одна конструкция; рядом/в панели раскрываются её
  стороны.
- Пользователь выбирает период, одну или несколько сторон, затем отправляет одну
  заявку со списком выбранных сторон и периодом.
- Занятые стороны можно выбирать, но они явно помечены предупреждением. В CRM
  сохраняется статус занятости выбранной стороны на момент заявки.
- Занятость на публичном сайте = любые не-отменённые брони, пересекающие
  выбранный период. Если пересечений несколько, показываем интервалы:
  `Занято: 01–05.08, 20–25.08`.
- Из публичной заявки автоматически не создаём брони; бронь создаёт менеджер
  после контакта.
- Экспорт аналитики в CSV/Excel не входит в первый проход.

## Открытые развилки

- Нет открытых продуктовых развилок по следующему проходу Этапа 2; можно
  переходить к реализации.

## Реализация MVP (2026-07-09)

- `Construction.side` удалён из модели; добавлен `Construction.sideCount` (1/2).
  Сторона A/B теперь хранится только на `Booking.side`.
- Добавлен контракт `packages/contracts/src/booking.ts`: статусы, типы, справочники,
  helper `addBookingMonths`, дефолт напоминания и схемы list/upsert.
- Добавлены Prisma-модели `Booking`, `BookingBrand`, `BookingServiceReason`; на
  момент MVP init-миграция была пересобрана до первого прод-деплоя.
- Backend:
  - `/api/bookings` — список, карточка, создание, правка, отмена;
  - `/api/booking-brands` и `/api/booking-service-reasons` — CRUD справочников;
  - проверка пересечений — в сервисе, `Serializable`-транзакция;
  - архив конструкции блокируется текущими/будущими не-отменёнными бронями,
    жёсткое удаление — любыми бронями.
- CRM:
  - новый раздел «Брони» с таймлайном/Гантом и формой брони;
  - новые справочники «Бренды» и «Причины броней»;
  - редактор конструкции теперь выбирает «Односторонняя / Двусторонняя (A/B)».
- Сид: добавлены стартовые причины служебных броней (ремонт, своя реклама,
  резерв, демонтаж).

## Проход Этапа 2: стороны, аналитика, публичная занятость (2026-07-09)

- Контракты:
  - `Construction.sideCount` расширен до `1/2/3`, сторона — код `A/B/C`;
  - добавлен DTO реальной стороны конструкции: `id`, `code`,
    направление/описание, price override, effective price, `trafficPerDay`,
    `grp`, фото;
  - `Booking.side` теперь DTO стороны, а сохранение брони принимает
    `constructionSideId` (legacy `side`-код оставлен как fallback);
  - добавлены контракты `inventory-analytics` и `public-availability`.
- Prisma:
  - добавлена модель `ConstructionSide`;
  - `Booking` переведён на обязательный `constructionSideId`;
  - новая обычная миграция:
    `backend/prisma/migrations/20260709090000_construction_sides/migration.sql`;
  - миграция одноразово расширяет известные demo-конструкции (`ББ-021`,
    `ПЛ-006`) до 3 сторон до backfill и проставляет стартовые описания/override
    сторон; регулярный seed не перетирает живые правки сторон;
  - добавлен composite FK `Booking(constructionSideId, constructionId)` →
    `ConstructionSide(id, constructionId)`, чтобы бронь не могла ссылаться на
    сторону другой конструкции;
  - init-миграция не переписывалась.
- Backend:
  - сохранение конструкции синхронизирует стороны A/B/C и запрещает уменьшать
    `sideCount`, если на удаляемой стороне есть брони; проверка удаляемых сторон
    делается по свежей строке внутри `Serializable` transaction;
  - создание/правка брони резолвит конкретную сторону, для односторонней
    конструкции использует A без выбора в UX; конфликтующие/невалидные explicit
    `constructionSideId` и legacy `side` не падают на другую сторону;
  - пересечения считаются по `constructionSideId` и периоду среди всех
    неотменённых броней;
  - PATCH брони без явных полей цены сохраняет текущий snapshot
    `basePricePerMonth`/`totalPrice`, а не пересчитывает его от каталога;
  - добавлен `/api/inventory-analytics` для CRM-аналитики загрузки/выручки;
  - добавлен `/api/public/construction-availability` без клиента/бренда/цены.
- CRM:
  - форма конструкции получила переключатель `1/2/3` и блоки сторон A/B/C;
  - числовые поля сторон/конструкции различают пустое значение и невалидный ввод
    (`42 000`, запятая в decimal принимаются; буквы/мусор показывают ошибку);
  - форма брони и таймлайн работают по реальным сторонам;
  - у multi-sided конструкций сторона в форме брони выбирается явно, автоподстановка
    A осталась только для односторонних;
  - в разделе «Аналитика» добавлена вкладка «Инвентарь» с периодом, быстрыми
    кнопками, карточками загрузки/выручки/свободных сторон/броней без цены и
    таблицей по сторонам, сгруппированной по конструкциям;
  - даты периодов/занятости в UI выводятся включительно.
- Сид:
  - demo-конструкции создают стороны A/B/C;
  - пилон «Черноречье» стал трёхсторонним;
  - у части сторон есть направления и ценовые переопределения.

## Проверки текущего прохода

- ✅ `PATH="$HOME/.bun/bin:$PATH" bun run typecheck`
- ✅ `PATH="$HOME/.bun/bin:$PATH" bun run test`
- ✅ `git diff --check`
- ✅ Фокусные регрессии добавлены для:
  - стороны брони: one-sided A, C-side overlap, explicit id vs legacy side;
  - price snapshot при PATCH брони;
  - запрета уменьшения сторон по свежей транзакционной строке;
  - очищенных side traffic/GRP;
  - публичной занятости и CRM inventory analytics;
  - product timezone date helpers, number parser, booking-side selection helper;
  - SQL-инвариантов новой migration.
- ⚠️ В backend-тестах во время зелёного прогона печатается ожидаемый fail-safe
  warning из Telegram-уведомления в `lead-service.test.ts`; suite зелёный.

## Риски / внимание после деплоя

- Прод-БД уже существует: init-миграцию не переписывать; следующие изменения
  схемы делать только новыми Prisma migrations.
- Миграция `20260709090000_construction_sides` применена на проде. Первый запуск
  упал на типизации `NULL` в backfill `INSERT ... UNION ALL`; фикс в commit
  `a8f2162` явно кастует `NULL::INTEGER`/`NULL::TEXT`. После проверки, что DDL
  откатился целиком (`ConstructionSide` отсутствовала, `Booking.constructionSideId`
  отсутствовал), миграция была помечена как rolled back через
  `bun x prisma migrate resolve --rolled-back 20260709090000_construction_sides`
  и успешно применена повторно.
- На проде создано 15 строк `ConstructionSide`; `_prisma_migrations` для
  `20260709090000_construction_sides` имеет `finished_at` и без `rolled_back_at`.
- Публичный агрегат занятости отдаёт только опубликованные неархивные
  конструкции и не выбирает клиент/бренд/цену.
- Из публичной заявки брони по-прежнему не создаются автоматически.

## Проверки

- ✅ `PATH="$HOME/.bun/bin:$PATH" bun run typecheck`
- ✅ `PATH="$HOME/.bun/bin:$PATH" bun run test`
- ✅ `git diff --check`
- ✅ `PATH="$HOME/.bun/bin:$PATH" bun test backend/test/construction-side-migration.test.ts`
- ✅ Продовый `bash infra/deploy-no-docker.sh`: `prisma migrate deploy`, seed,
  `website build`, `webapp build`, nginx config test и рестарты сервисов прошли.
- ⚠️ В backend-тестах во время зелёного прогона печатается ожидаемый fail-safe
  warning из Telegram-уведомления в `lead-service.test.ts`; suite зелёный.

## Обновление прода 2026-07-10: Этап 2 стороны/брони/аналитика

- Ветка `claude/stage-2-period-booking-design-14q994` запушена в origin.
  Последовательность коммитов релиза:
  - `06fa7c0` — основной Stage 2 side-based bookings;
  - `7296cfc` — no-Docker deploy теперь запускает Prisma migrations;
  - `a8f2162` — фикс кастов в migration backfill.
- Прод обновлён rsync-ом из локального workspace в `/var/www/noesis_adv`, затем
  выполнен `bash infra/deploy-no-docker.sh`.
- Smoke-checks после релиза:
  - `noesis-backend`, `noesis-site-builder`, `nginx`, `catalog-noema` active;
  - `curl http://127.0.0.1:3001/health` → `{"status":"ok"}`;
  - `https://noesis.catlg.ru/` → 200;
  - `https://noesis.catlg.ru/crm/` → 200;
  - `https://noesis.catlg.ru/api/public/site-stats` → JSON;
  - `https://noesis.catlg.ru/api/public/construction-availability?from=2026-08-01&to=2026-09-01`
    → JSON со сторонами и `busyIntervals`, без клиента/бренда/цены;
  - `https://catlg.ru/` остался 200;
  - в БД `SELECT COUNT(*) FROM "ConstructionSide";` → `15`;
  - migration `20260709090000_construction_sides` applied (`finished_at IS NOT NULL`).

## VPS-аудит и no-Docker деплой (2026-07-09)

- Проверен VPS `77.222.32.54`: Ubuntu 24.04, 1 vCPU, 1.9 GB RAM + 2 GB swap,
  диск 15 GB / свободно ~4.9 GB.
- Уже работает `catalog.noema`: systemd `catalog-noema.service`, Next.js на
  `127.0.0.1:3000`; nginx занимает `80/443` для `catlg.ru`, `www.catlg.ru`,
  `*.catlg.ru`; PostgreSQL 16 слушает `127.0.0.1:5432`.
- Docker и Bun на VPS отсутствуют; Node есть у `deploy` через nvm. Certbot и
  wildcard TLS `catlg.ru-0001` есть.
- Решение для подселения Noesis: не использовать Docker Compose напрямую, а
  запускать backend systemd-сервисом на `127.0.0.1:3001`, использовать отдельную
  БД/роль PostgreSQL, отдавать `website/web/current`, `webapp/dist` и `/files`
  через существующий host-nginx.
- Добавлена no-Docker дорожка:
  - `infra/no-docker.env.example`;
  - `infra/deploy-no-docker.sh`;
  - `infra/nginx/no-docker.conf.template`;
  - `infra/systemd/*.no-docker.service.template`;
  - npm-script `bun run deploy:vps:no-docker`.
- Боевой деплой выполнен на VPS:
  - URL сайта: `https://noesis.catlg.ru/`;
  - CRM: `https://noesis.catlg.ru/crm/`;
  - backend: systemd `noesis-backend`, `127.0.0.1:3001`;
  - site-builder: systemd `noesis-site-builder`;
  - nginx-site: `/etc/nginx/sites-available/noesis`;
  - БД/роль PostgreSQL: `noesis`;
  - секреты и временный пароль админа лежат только на VPS в
    `/var/www/noesis_adv/infra/no-docker.env` (`chmod 600`).
- В ходе деплоя:
  - поставлен пакет `unzip` для установщика Bun;
  - Bun установлен в `/usr/local/bin/bun`;
  - journal systemd очищен до ~300 MB, свободное место выросло до ~6 GB;
  - nginx-шаблон no-Docker переведён на обычный HTTPS без `http2 on`, потому что
    текущий nginx не знает директиву `http2`;
  - backend получил `HOST=127.0.0.1`, чтобы порт `3001` не слушал внешний интерфейс.
- Smoke-checks после деплоя:
  - `noesis-backend`, `noesis-site-builder`, `nginx`, `catalog-noema` active;
  - `curl http://127.0.0.1:3001/health` → `{"status":"ok"}`;
  - `https://noesis.catlg.ru/` → 200;
  - `https://noesis.catlg.ru/crm/` → 200;
  - `https://noesis.catlg.ru/api/public/site-stats` → JSON;
  - login первого админа через HTTPS API → HTTP 200;
  - `https://catlg.ru/` остался 200.

## Следующие smoke-тесты

1. В CRM создать новую трёхстороннюю конструкцию, заполнить A/B/C, выбрать фото
   стороны из галереи, сохранить и открыть карточку повторно.
2. Создать бронь на односторонней конструкции: в форме сторона не должна
   выбираться, но таймлайн должен показать строку стороны A.
3. Создать бронь на A, проверить конфликт пересечения на A и успешную бронь на B
   или C, включая стык-в-стык.
4. Попробовать уменьшить `3 → 2` при бронях на C и `2 → 1` при бронях на B:
   ожидать 409 и понятную ошибку.
5. Проверить аналитику «Инвентарь»: service/cancelled не входят в выручку,
   commercial без `totalPrice` попадает в «Брони без цены», загрузка считается
   по сторонам/дням.
6. Проверить
   `/api/public/construction-availability?from=YYYY-MM-DD&to=YYYY-MM-DD`:
   нет клиента/бренда/цены, статусы `free/partiallyOccupied/occupied`, интервалы
   отображаются включительно.

## Как обновлять прод в новой сессии

Текущий прод на VPS — **не git checkout**: код был синхронизирован rsync-ом из
локального workspace в `/var/www/noesis_adv`, без `.git`, `node_modules`,
локальных env и build-артефактов. Секреты живут только на VPS в
`/var/www/noesis_adv/infra/no-docker.env` и не должны перетираться.

1. Локально в workspace:

   ```bash
   cd /Users/ibrahim/Documents/Codex/noesis_adv
   git status --short
   PATH="$HOME/.bun/bin:$PATH" bun run typecheck
   PATH="$HOME/.bun/bin:$PATH" bun run test
   ```

2. Синхронизировать код на VPS (пароль/ключ SSH — у пользователя; секретный env
   исключён и останется на сервере):

   ```bash
   rsync -az --delete \
     --exclude='.git/' \
     --exclude='node_modules/' \
     --exclude='**/node_modules/' \
     --exclude='website/dist/' \
     --exclude='website/web/' \
     --exclude='webapp/dist/' \
     --exclude='backend/src/generated/' \
     --exclude='.astro/' \
     --exclude='.vite/' \
     --exclude='coverage/' \
     --exclude='playwright-report/' \
     --exclude='test-results/' \
     --exclude='infra/no-docker.env' \
     --exclude='infra/.env' \
     --exclude='infra/.site-build.lock' \
     --exclude='*.tsbuildinfo' \
     ./ root@77.222.32.54:/var/www/noesis_adv/
   ```

3. На VPS запустить no-Docker deploy:

   ```bash
   ssh root@77.222.32.54
   cd /var/www/noesis_adv
   bash infra/deploy-no-docker.sh
   ```

## Обновление прода 2026-07-09: публичный сайт конструкций + карта

- Прод обновлён по no-Docker инструкции выше: rsync локального workspace →
  `bash infra/deploy-no-docker.sh` на `root@77.222.32.54`.
- Первый деплой выкатил код, но БД сохранила старый demo-контент (`AURUM`,
  `GREEN City` и т. п.), потому что сид был idempotent и пропустил существующие
  конструкции.
- Добавлен безопасный refresh в `backend/prisma/seed-content.ts`: если БД состоит
  ровно из старого demo-набора slugs, сид обновляет записи in-place под наружную
  рекламу, сохраняя id конструкций и связи заявок/броней.
- Повторный деплой успешно обновил demo-контент и пересобрал сайт. Сгенерированы
  новые страницы:
  `/constructions/siti-format-prospekt-putina/`,
  `/constructions/siti-format-ploschad-minutka/`,
  `/constructions/schit-ahmat-arena/` и остальные outdoor-конструкции.
- Smoke-checks после деплоя:
  - `https://noesis.catlg.ru/` → 200;
  - `https://noesis.catlg.ru/crm/` → 200;
  - `https://noesis.catlg.ru/constructions/siti-format-prospekt-putina/` → 200;
  - API `/api/public/constructions` → 8 конструкций с координатами;
  - главная не содержит `AURUM`, `GREEN City`, `ЖК`, `недвиж`;
  - sitemap содержит 8 `/constructions/` URL и не содержит `/constructions/aurum`;
  - `noesis-backend`, `noesis-site-builder`, `nginx`, `catalog-noema` active;
  - `curl http://127.0.0.1:3001/health` на VPS → `{"status":"ok"}`;
  - соседний `https://catlg.ru/` остался 200.

   Скрипт идемпотентный: переиспользует БД/роль/env, ставит зависимости,
   применяет Prisma migrations, сидит справочники, пересобирает website/webapp,
   рендерит nginx-config и рестартует systemd-сервисы.

4. Проверить прод:

   ```bash
   systemctl is-active noesis-backend noesis-site-builder nginx catalog-noema
   ss -ltnp | egrep '3000|3001|80|443|5432'
   curl -fsS http://127.0.0.1:3001/health
   curl -fsSI https://noesis.catlg.ru/
   curl -fsSI https://noesis.catlg.ru/crm/
   curl -fsS https://noesis.catlg.ru/api/public/site-stats
   curl -fsSI https://catlg.ru/
   ```

Ожидаемо: `noesis-backend` слушает `127.0.0.1:3001`, `catalog-noema` остаётся
на `127.0.0.1:3000`, `catlg.ru` не должен измениться.

## Технические ориентиры / инварианты
- Прод-БД уже есть на VPS. Новые изменения схемы делать обычной Prisma-миграцией;
  init-миграцию больше не схлопывать и не переписывать под прод.
- Единый инстанс на VPS; in-memory троттлинг; фоновых демонов нет (cron — только бэкапы).
  Авто-снятия/авто-переходы делаем «лениво» на чтении, не демоном.
- Занятость и права — в сервисе, не в БД (кроме, возможно, `EXCLUDE`-констрейнта, п.7).
- Гейт перед деплоем: `typecheck` + `test` по всему монорепо.
